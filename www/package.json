{
  "name": "w3f-registrar-frontend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.0.1",
    "websocket-heartbeat-js": "^1.0.13"
  },
  "scripts": {
    "build": "tsc --outDir build/ && mkdir -p dist/assets && cp -R assets dist/ && node-sass src/ -o dist/assets && browserify build/index.js -o dist/assets/bundle.js && cp favicon.ico dist/ && cp *.html dist/"
  },
  "devDependencies": {
    "node-sass": "^6.0.0"
  }
}
